<!-- Navbar.astro -->
<nav class="navbar">
  <ul class="nav-links">
    <li class="nav-item active">
      <a href="#home"><span>Home</span></a>
    </li>
    <li class="nav-item">
      <a href="#about"><span>About</span></a>
    </li>
    <li class="nav-item">
      <a href="#skills"><span>Skills</span></a>
    </li>
    <li class="nav-item">
      <a href="#projects"><span>Projects</span></a>
    </li>
    <li class="nav-item">
      <a href="#contact"><span>Contact</span></a>
    </li>
  </ul>
</nav>

<style>
/* Sidebar / Navbar */
.navbar {
  position: fixed;
  width: 200px;
  height: calc(100vh - 72px);
  z-index: 1000;
  backdrop-filter: blur(10px);
  padding-left: 20px;
  transition: left 0.3s ease;
  display: flex;
  align-items: center;
}

.nav-links {
  display: flex;
  flex-direction: column;
  gap: 30px;
  align-items: flex-start;
}

.nav-links li {
  color: #c4c4c4;
  display: flex;
  align-items: center;
  transition: all 0.3s ease;
  opacity: 0.8;
  padding-left: 30px;
  position: relative;
  z-index: 1000;
}

.nav-links li.active {
  color: #ffffff;
  opacity: 1;
  padding-left: 50px;
  transition: padding-left 0.3s ease;
}

.nav-links li.active::before {
  content: "";
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  height: 2px;
  z-index: 0;
  background: linear-gradient(45deg, #007bff, #e35cbb);
  transition: width 0.5s ease-in-out;
  width: calc(100% - 90px); 
}

.nav-links a {
  z-index: 1;
  display: inline-block;
  width: 100PX;
  position: relative;
}

.nav-links a:before {
  content: "✶";
  font-size: 1.5rem;
  color: #ea8e8b;
  margin-right: 10px;
  z-index: 1;
  position: relative;
  display: inline-block;
}

/* Responsive Navbar */
@media (max-width: 768px) {
  .navbar {
    padding: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    margin: 0;
    width: 70px;
    right: -200px;
    transition: right 0.3s ease;
  }

  .navbar.open {
    right: 0px;
  }


  .nav-links{
    height: inherit;
    
    gap: 0;

    width: 70px;
  }
  .nav-links li {
    width: 70px;
    border-left: 1px solid white;
    writing-mode: vertical-lr;
    text-align: center;
    color: #fff;
    opacity: 1;
    position: relative; 
    margin: 0;
    padding: 0;
    
    padding: 17px 0 ;
    width: 100%;
  }

  .nav-links a {
    margin: 0;
    width: auto;
    height: 90px;
    padding: 0;
  }

  .nav-links li.active {
    padding-left: 0px;
    border-left: transparent;
    color: #ea8e8b;
    position: relative; 
  }

  .nav-links li.active::before {
    content: '';
    position: absolute;
    left: -117px; /* Ajusta según necesites */
    top: 50%;
    transform: translateY(-50%) rotate(-90deg);
    width: 146px;
    height: 90px;
    background-image: url('/public/tumor.svg');
    clip-path: var('/public/tumor.svg');
    background-size: cover;
    background-repeat: no-repeat;
    background-color: transparent;
  backdrop-filter: blur(10px);
    z-index: 1005;
    /* No es necesario backdrop-filter */
}


  .navbar::before {
    background: none;
  }
  

  .nav-links a:before {
    font-size: 1.2rem;
  }

</style>
