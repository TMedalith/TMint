<!-- Navbar.astro -->
<nav class="navbar">
  <ul class="nav-links">
    <li class="nav-item active">
      <a href="#home"><span>Home</span></a>
    </li>
    <li class="nav-item">
      <a href="#about"><span>About</span></a>
    </li>
    <li class="nav-item">
      <a href="#skills"><span>Skills</span></a>
    </li>
    <li class="nav-item">
      <a href="#projects"><span>Projects</span></a>
    </li>
    <li class="nav-item">
      <a href="#contact"><span>Contact</span></a>
    </li>
  </ul>
</nav>

<style>
/* Sidebar / Navbar */
.navbar {
  position: fixed;
  width: 200px;
  height: calc(100vh - 72px);
  z-index: 1000;
  backdrop-filter: blur(10px);
  background-color: var(--background-dark);
  padding-left: 20px;
  transition: left 0.3s ease;
  display: flex;
  align-items: center;
}

.nav-links {
  display: flex;
  flex-direction: column;
  gap: 30px;
  align-items: flex-start;
}

.nav-links li {
  color: #c4c4c4;
  display: flex;
  align-items: center;
  transition: all 0.3s ease;
  opacity: 0.8;
  padding-left: 30px;
  position: relative;
  z-index: 1000;
}


.nav-links a{
  width: 100px;
}

.nav-links li.active {
  color: #ffffff;
  opacity: 1;
  padding-left: 50px;
  transition: padding-left 0.3s ease;
}

/* Adjust line width to stop before the star */
.nav-links li.active::before {
  content: "";
  position: absolute;
  left: 0; /* Start the line from the left edge */
  top: 50%;
  transform: translateY(-50%);
  height: 2px;
  z-index: 0;
  background: linear-gradient(45deg, #007bff, #e35cbb);
  transition: width 0.5s ease-in-out;
  width: calc(100% - 90px); /* Total width minus star size and margin-right */
}

/* Star styling */
.nav-links a:before {
  content: "✶";
  font-size: 1.5rem; /* Size of the star */
  color: #ea8e8b;
  margin-right: 10px; /* Margin between the star and text */
  z-index: 1;
  position: relative;
  display: inline-block;
}

.nav-links a {
  z-index: 1;
  display: inline-block;
  position: relative;
}

/* Responsive Navbar */
@media (max-width: 768px) {
  .navbar {
    width: 80px;
    right: -200px;
    transition: right 0.3s ease;
  }

  .navbar.open {
    right: 0;
  }

  .nav-links li {
    writing-mode: vertical-lr;
    text-align: center;
  }

  .nav-links li.active::before {
    left: -10px;
    width: calc(100% + 10px);
  }
}
</style>
